// Shared non-component exports for calendar (hooks, types, constants)
export { useItineraryCalendar } from '../../hooks/useItineraryCalendar';

export type {
  EnhancedItineraryItem,
  ActivityType,
  ActivityFilter,
  CalendarEvent,
  CalendarViewConfig,
  TripStatistics
} from '../../types/trip';
